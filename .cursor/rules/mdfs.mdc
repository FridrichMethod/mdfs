---
alwaysApply: true
---

# Cursor Rules

## Environment

- Work inside the shared conda env: `conda activate mdfs` (Python >= 3.12). GPU workflows assume CUDA 12 capable drivers.
- **Make sure the conda environment `mdfs` is activated before you run ANY commands!**
